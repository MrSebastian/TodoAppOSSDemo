name: verify frontend main push

on:
  push:
    branches:
      - 'main'
    paths:
      - 'frontend/**'

jobs:
  checkout_and_verify:
    uses:
      ./.github/workflows/callable-run-mvn-verify.yml
    with:
      pom-dir: 'frontend'