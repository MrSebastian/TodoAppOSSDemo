name: verify backend main push

on:
  push:
    branches:
      - 'main'
    paths:
      - 'backend/**'

jobs:
  checkout_and_verify:
    uses:
      ./.github/workflows/callable-run-mvn-verify.yml
    with:
      pom-dir: 'backend'