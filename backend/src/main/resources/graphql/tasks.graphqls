type Task {
    id: ID!
    title: String!,
    description: String!,
    dueDate: String
}

type Person {
    id: ID!
    firstname: String
    lastname: String,
    email: String!
}

type Query {
    getTasks: [Task]!
    getPersons: [Person]!
}